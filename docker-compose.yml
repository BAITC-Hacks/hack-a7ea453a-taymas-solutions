services:
  pipeline:
    build:
      context: .
      dockerfile: Dockerfile
      target: pipeline
    image: ${PIPELINE_IMAGE:-money-graph-pipeline:local}
    user: "${LOCAL_UID:-10001}:${LOCAL_GID:-10001}"
    command: ["--data", "/app/data", "--out", "/app/out", "--check-repro"]
    volumes:
      - type: bind
        source: ${DATA_DIR:-./data}
        target: /app/data
        read_only: true
      - type: bind
        source: ${OUT_DIR:-./out}
        target: /app/out
    restart: "no"

  copilot:
    build:
      context: .
      dockerfile: Dockerfile
      target: pipeline
    image: ${PIPELINE_IMAGE:-money-graph-pipeline:local}
    user: "${LOCAL_UID:-10001}:${LOCAL_GID:-10001}"
    entrypoint: ["python", "-m", "agent_orchestrator.server"]
    command: ["--out", "/app/out", "--host", "0.0.0.0"]
    depends_on:
      pipeline:
        condition: service_completed_successfully
    environment:
      NVIDIA_API_KEY: ${NVIDIA_API_KEY:-}
      NVIDIA_MODEL: ${NVIDIA_MODEL:-}
    volumes:
      - type: bind
        source: ${OUT_DIR:-./out}
        target: /app/out
        read_only: true
    healthcheck:
      test: ["CMD", "python", "-c", "import json, urllib.request; r=json.load(urllib.request.urlopen('http://127.0.0.1:8765/api/copilot/status', timeout=2)); assert r['ready'] is True"]
      interval: 3s
      timeout: 3s
      start_period: 5s
      retries: 20
    restart: unless-stopped

  ui:
    build:
      context: .
      dockerfile: Dockerfile
      target: ui
    image: ${UI_IMAGE:-money-graph-ui:local}
    depends_on:
      pipeline:
        condition: service_completed_successfully
    ports:
      - "127.0.0.1:${UI_PORT:-8501}:80"
    volumes:
      - type: bind
        source: ${OUT_DIR:-./out}
        target: /usr/share/nginx/html/out
        read_only: true
    restart: unless-stopped
